let myarray = ['1stValue','2ndValue','3rdValue','4thValue'];

let [one, two, three, four] = myarray;
console.log(three);

let organization={
    name:"sanket",
    address:{
        landmark:"shivajinagar",
        pin:431001
    }
};
console.log(organization.address.pin);